# 🌿 Mủ Cao Su Pro

Phần mềm ghi chép **mua bán mủ cao su** — PWA cài được lên điện thoại như app thật, hoạt động offline.

## 🔢 Quy tắc đánh số phiên bản (hệ hiện hành: `1.X.Y`, áp dụng từ v1.1.0)

Số phiên bản theo dạng **`1.X.Y`**:

- Số hàng cuối cùng bên phải (**Y**) tăng đúng **1 đơn vị** mỗi lần cập nhật, chạy từ **0 → 9**.
- Hết 9 thì **X** (số hàng chục/ở giữa) tăng lên 1, **Y** quay lại **0**.
- Ví dụ thứ tự: `1.8.0` → `1.8.1` → ... → `1.8.9` → `1.9.0` → `1.9.1` → ...
- Mỗi lần cập nhật chỉ tăng đúng 1 bước theo đúng thứ tự trên, áp dụng ở **4 vị trí**: `<title>` + `logo-ver` + dòng hiển thị trong màn "Hướng dẫn sử dụng" (trong `index.html`), `description` trong `manifest.json`, và `CACHE` trong `sw.js` — sau khi đổi, luôn `grep` số cũ để chắc chắn không sót vị trí nào.
- Các ghi chú phiên bản cũ hơn (hệ `v7.x`–`v11.14`, rồi hệ chữ-số `1.<chữ><số>` như `1.a1`...`1.b5`) còn sót lại trong code/bảng lịch sử bên dưới chỉ là mốc lịch sử, không quy đổi ngược.

## 📋 Lịch sử phiên bản

| Phiên bản | Ngày | Thay đổi |
|-----------|------|----------|
| **v1.9.5** | 2026-08-05 | 📷 **Ảnh đính kèm giao dịch**: đổi mặc định số ngày tự xoá từ 30 → **7 ngày**, giới hạn tối đa cho phép chọn từ 365 → **30 ngày** (nhập quá 30 sẽ tự điều chỉnh lại và báo rõ trong thông báo). Không ảnh hưởng ảnh đã tải lên từ trước, chỉ áp dụng cho ảnh tải lên sau khi cập nhật |
| **v1.9.4** | 2026-08-05 | 🔍 **Rà soát toàn diện sau nhiều lần chỉnh sửa** — kiểm tra cú pháp JS, cân bằng thẻ HTML, id trùng lặp, hàm bị gọi nhưng không tồn tại, tên setting không khớp giữa nơi đọc/ghi. Kết quả: mọi thay đổi gần đây (bong bóng, đọc số tiền, ẩn bớt form) đều hoạt động đúng, không phát sinh lỗi mới. Chỉ phát hiện 1 lỗi tiềm ẩn từ code cũ (`toggleSgDrop()` thiếu kiểm tra null) — hàm này thực tế chưa được gắn vào nút nào nên không ảnh hưởng người dùng, đã vá phòng ngừa |
| **v1.9.3** | 2026-08-04 | 🫧 **Bong bóng "nổ" sau 10 lần va chạm**: mỗi quả bóng va chạm đủ 10 lần sẽ tự nổ tung (hiệu ứng giống hệt lúc chạm màn hình để đóng: vòng màu bùng nổ chậm rãi + mảnh vỡ bắn ra ngoài), đồng thời **lập tức sinh ra 1 quả bóng khác** ở vị trí ngẫu nhiên khác để thế vào ngay, giữ nguyên tổng số lượng — giúp giải thoát các quả bóng lỡ bị mắc kẹt va đi va lại liên tục ở một góc màn hình (logo không áp dụng cơ chế này, luôn giữ nguyên). 📋 **Tính năng mới: Ẩn bớt khi chưa nhập liệu** (⚙️ Cài đặt, mặc định TẮT) — khi bật, ô "Thành tiền" + hàng nút Lưu/Huỷ ở tab Mua và Bán sẽ tự ẩn đi lúc chưa nhập đủ dữ liệu để tính ra thành tiền (tiết kiệm không gian xem chi tiết thu mua bên dưới), và tự hiện đầy đủ trở lại ngay khi nhập đủ; sau khi lưu hoặc huỷ giao dịch thành công lại ẩn về như cũ |
| **v1.9.2** | 2026-08-04 | 🐛 **Sửa 3 lỗi**: (1) Tab Bán ra chưa từng đọc to số tiền sau khi lưu — code chỉ kiểm tra `tab==='mua'`, bỏ sót tab bán; nay áp dụng cho cả 2 tab. (2) Đọc số tiền ở tab Mua bị trễ 1 khoảng mới bắt đầu đọc (lỗi thường gặp của Chrome/Android) — đã thêm thủ thuật `pause()+resume()` ngay sau `speak()` để ép đọc ngay lập tức, và tải sẵn danh sách giọng đọc từ lúc mở app thay vì đợi lần đọc đầu tiên. (3) Logo đồng xu bị khựng lại 1 chút trước khi xoay tiếp mỗi khi bay về đúng vị trí trên header — do bản sao bay ra và logo header chạy animation xoay độc lập, lệch pha nhau nên khi hoán đổi qua lại góc xoay bị nhảy đột ngột; đã đồng bộ pha xoay giữa 2 phần tử ngay từ lúc bắt đầu bay ra, xoay liền mạch không còn khựng |
| **v1.9.1** | 2026-08-04 | 🫧 **Làm nổi bật hiệu ứng vỡ bong bóng**: trước đó diễn ra quá nhanh (~0.3s) khó nhận ra là "vỡ". Nay vòng màu lan toả **chậm rãi hơn nhiều**, kèm thêm **6-8 mảnh vỡ nhỏ bắn tung ra ngoài** từ tâm mỗi quả rồi chậm dần, mờ dần (giống mảnh bong bóng xà phòng thật). Kéo dài thời gian trước khi ẩn hẳn overlay (từ 0.54s lên 0.95s) để hiệu ứng chậm hơn có đủ thời gian chạy hết trước khi vào lại app |
| **v1.9.0** | 2026-08-03 | 🫧 **Hiệu ứng tắt bong bóng bảo vệ màn hình mượt hơn**: khi chạm màn hình để tắt, các quả bóng thường nay **"vỡ ra"** (vòng màu bùng nổ nhanh rồi tan biến) thay vì biến mất đột ngột; logo app **bay ngược trở lại đúng vị trí xoay đồng xu ban đầu** trên header (thay vì biến mất tức thì). Đồng thời sửa để icon xoay đồng xu ở vị trí cũ trên header **tự ẩn đi ngay khi bản sao đã bay ra ngoài** lúc bong bóng vừa xuất hiện (trước đó bị hiện trùng 2 logo cùng lúc), và hiện lại đúng lúc logo bay về tới nơi |
| **v1.8.9** | 2026-08-03 | 🫧 **Bỏ hẳn tuỳ chỉnh thời gian chờ** của bong bóng bảo vệ màn hình (từng gây lỗi khi đặt 5-10 giây) — cố định lại **20 giây**, không cho chỉnh trong Cài đặt nữa. Chỉ còn 2 tuỳ chọn: bật/tắt hiệu ứng và bật/tắt âm thanh va chạm |
| **v1.8.8** | 2026-08-03 | 🚨 **Sửa lỗi nghiêm trọng: app hiện toàn bộ code khi mở** — do bản v1.8.7 có 1 dòng comment giải thích lỗi vô tình chứa đúng chuỗi ký tự `</script>` (dùng để mô tả kỹ thuật) nằm ngay bên trong khối `<script>` chính của app. Trình duyệt không phân biệt được đó là text trong comment hay thẻ đóng thật — hễ gặp đúng chuỗi `</script>` là đóng thẻ script ngay lập tức, bất kể đang ở trong comment hay chuỗi ký tự. Điều này khiến toàn bộ code phía sau bị trình duyệt hiển thị thẳng ra màn hình dưới dạng text thay vì chạy như JavaScript. Đã xoá chuỗi ký tự gây lỗi khỏi comment, xác nhận lại toàn bộ file chỉ còn đúng 1 khối `<script>` hợp lệ duy nhất |
| **v1.8.7** | 2026-08-03 | 🐛 **Sửa dứt điểm lỗi không thoát được bong bóng bảo vệ màn hình**: bản v1.8.6 gắn listener chặn sự kiện trực tiếp lên phần tử overlay ngay khi script chạy, nhưng phần tử đó nằm ở cuối `<body>` — **sau** thẻ đóng script — nên lúc đó phần tử chưa tồn tại trong DOM, khiến listener **không bao giờ được gắn** (lỗi vẫn còn y nguyên dù tưởng đã sửa). Đã đổi sang lắng nghe ở `document` với `capture: true`, luôn hoạt động ngay từ đầu bất kể thứ tự phần tử trong trang, chặn hẳn sự kiện chạm khi bong bóng đang hiện. 🐛 **Sửa lỗi viền trắng quanh logo bay**: thêm `border-radius:50%` trực tiếp lên thẻ `<img>` của logo (giống hệt logo header) — khi ảnh đang xoay 3D, việc cắt hình tròn chỉ dựa vào `overflow:hidden` của phần tử cha không đáng tin cậy trên nhiều trình duyệt, để lộ góc vuông nền trắng của ảnh gốc |
| **v1.8.6** | 2026-08-03 | 🐛 **Sửa lỗi nghiêm trọng**: chạm để tắt bong bóng bảo vệ màn hình bị "lọt" xuống bấm nhầm nút/phần tử bên dưới (do sự kiện tắt overlay không chặn hẳn lại), gây cảm giác không thoát ra được để dùng app — nhất là khi đặt thời gian chờ ngắn (5-10s) vì lặp lại liên tục. Đã chặn hẳn sự kiện chạm ngay tại overlay (preventDefault + stopPropagation). 🫧 **Trang trí bong bóng bảo vệ màn hình đầy đủ**: đổ bóng mềm phía sau mỗi quả cho nổi khối; vệt chuyển động (motion trail) mờ dần theo hướng di chuyển; lấp lánh ngẫu nhiên trên bề mặt; nền gradient rất nhẹ đổi màu chậm theo thời gian (không che app); âm thanh "pop" nhỏ khi va chạm (bật/tắt được trong Cài đặt); chữ "Chạm màn hình để tiếp tục" nhấp nháy mờ-hiện nhẹ nhàng; số lượng bong bóng co giãn theo diện tích màn hình (điện thoại nhỏ ít hơn, tablet nhiều hơn) |
| **v1.8.1 → v1.8.5** | 2026-08-03 | 🫧 **Chuỗi điều chỉnh hiệu ứng "Bong bóng bảo vệ màn hình"** (chỉ tinh chỉnh, không phải tính năng mới): nền phía sau đổi từ màu đặc sang **trong suốt hoàn toàn** để thấy rõ app đang chạy phía sau; thêm **va chạm giữa các quả bóng** (tự tách ra + nẩy ngược hướng thay vì xuyên qua nhau); giảm số lượng bóng còn **một nửa** và bỏ cỡ lớn nhất (nay 14-42px) cho có không gian di chuyển, phù hợp màn hình điện thoại; giảm bớt tốc độ tăng mỗi lần va chạm và giới hạn tốc độ tối đa cho nẩy nhẹ nhàng hơn; tăng nhẹ tốc độ di chuyển ban đầu; mỗi lần va chạm nay **đổi màu ngẫu nhiên** cả 2 quả + hiện **hiệu ứng vòng sáng lan toả** tại điểm va chạm |
| **v1.8.0** | 2026-08 | **🫧 Tính năng mới: Bong bóng bảo vệ màn hình** — giống hiệu ứng "Bubbles" screensaver kinh điển của Windows: hàng chục bong bóng trong suốt, nhiều màu, tự nảy quanh màn hình. Tự kích hoạt sau **15 giây không chạm/gõ/cuộn** vào app, chạm bất kỳ đâu để tắt ngay. Bật/tắt tại ⚙️ Cài đặt → "🫧 Bong bóng bảo vệ màn hình" — **mặc định TẮT** khi mới cài. Tự dừng hẳn khi chuyển sang app khác/khoá máy (đỡ tốn pin), không chạy trong nền |
| **v1.7.3** | 2026-08 | 🔍 **Quét lỗi toàn diện, tìm và sửa 12 lỗi:** (1) **Hàm `updBanHints()` bị định nghĩa trùng 2 lần** — bản cũ (thiếu tính năng "gợi ý giá từ lịch sử giá đã lưu") nằm SAU trong file nên âm thầm đè mất bản mới hơn, khiến gợi ý giá theo lịch sử ở tab Bán Ra chưa từng thực sự hoạt động — đã gỡ bản cũ. (2) Hàm `chupAnhSS()` cũng bị định nghĩa trùng 2 lần (1 bản tạm chưa hoàn thiện + 1 bản thật dùng html2canvas) — đã gỡ bản tạm cho gọn code. (3-10) **8 hàm xoá/sửa dữ liệu thiếu `autoSync()`** (chỉ đồng bộ lên Supabase qua vòng đồng bộ ngầm 15 giây thay vì ngay lập tức): `xoaGD`, `xoaKhachId`, `luuSgEdit`, `xoaSgEdit`, `xoaGhiChepSS`, `xoaChotKH`, `xoaCc`, `xoaSgEntry` — nay đều đồng bộ ngay sau khi thao tác thành công. (11-12) **Khoá nhất quán khi hết hạn dùng**: `luuSgEdit`, `xoaSgEdit`, `xoaSgEntry`, `xoaChotKH` (Sổ Gửi/Ứng tiền) trước đây KHÔNG bị khoá dù đã hết hạn — trong khi Thu Mua/Bán Ra thì có khoá — nay đã thêm khoá nhất quán, hết hạn thì các thao tác này cũng bị chặn như Thu Mua/Bán Ra. Đã rà soát thêm: không còn ID trùng lặp trong HTML, không còn hàm được gọi mà thiếu định nghĩa, các modal đều khớp id |
| **v1.7.2** | 2026-08 | 🐛 **Sửa lỗi thật sự khiến "🔧 Trạng thái đồng bộ máy này" mãi kẹt ở "Chưa từng chạy"** dù máy có mạng đầy đủ (khác với nguyên nhân RLS đã đoán ở bản trước — đã xác minh lại bằng ảnh chụp thực tế). Nguyên nhân đúng: hàm `verifyLicenseAntiCheat()` có điều kiện kiểm tra `navigator.onLine` ở đầu hàm để tránh gọi mạng khi biết chắc đang offline — nhưng cờ `navigator.onLine` **không đáng tin cậy** trên nhiều trình duyệt/WebView Android, có thể báo sai "offline" ngay lúc app vừa mở dù mạng vẫn tốt, khiến cả hàm **âm thầm không chạy gì cả**, không lỗi không thành công, kẹt mãi ở trạng thái ban đầu. Đã bỏ hẳn điều kiện kiểm tra này (cứ để hàm thử gọi thật), đồng thời khi mất mạng thật sự thì nay sẽ **báo lỗi rõ ràng + tự động thử lại sau 20 giây** thay vì im lặng bỏ qua như trước |
| **v1.7.1** | 2026-08 | 🐛 **Sửa lỗi quan trọng: máy mới đăng nhập Supabase không tự đồng bộ giao dịch mới.** Nguyên nhân: `sbSignIn()`/`sbSignUp()` (hàm xử lý khi bấm Đăng nhập/Đăng ký) thiếu bước ghi nhận "phương án đồng bộ đang dùng là Supabase" (`sync_provider`) trên chính máy đó — khiến `autoSync()` sau mỗi lần lưu giao dịch kiểm tra thấy phương án đồng bộ trống nên **âm thầm không làm gì cả** (không lỗi, không thông báo), giao dịch chỉ nằm im trên máy cho tới khi người dùng tự tay bấm "Đồng bộ ngay". Đã bổ sung bước ghi nhận này ngay khi đăng nhập/đăng ký thành công — từ nay máy mới đăng nhập xong là tự đồng bộ ngay sau mỗi giao dịch như bình thường, không cần thao tác thủ công |
| **v1.7.0** | 2026-08 | Khối **"📷 Ảnh đính kèm"** ở tab Thu Mua/Bán Ra nay **thu gọn mặc định** — chỉ hiện dòng tiêu đề, chạm vào mới mở ra 2 nút "📷 Chụp ảnh"/"🖼️ Chọn ảnh có sẵn" (đỡ chiếm chỗ màn hình khi không có nhu cầu đính kèm ảnh). Khi sửa 1 giao dịch đã có sẵn ảnh, khối này tự động mở ra ngay để thấy ảnh đã đính kèm, không cần chạm thủ công. *(Bản vá này áp dụng trên nền file v1.6.9 do người dùng cung cấp — README có thể thiếu mục lịch sử cho các phiên bản 1.6.5 → 1.6.9 nếu có thay đổi chưa được ghi lại ở đây.)* |
| **v1.6.4** | 2026-07 | Viết lại màn "📁 Thư mục lưu trữ Supabase" thành **2 tab riêng biệt ngay từ đầu**: **💾 Dữ liệu** (bảng mucaosu_data, thanh dung lượng theo quota Database 500MB) và **🖼️ Ảnh** (bucket Storage giao-dich-anh, thanh dung lượng theo quota Storage 1GB) — mỗi tab hiện thanh dung lượng riêng đúng loại quota của tab đó, bấm chuyển tab không phải mở lại màn từ đầu. *Lưu ý: ảnh tải lên trước v1.6.3 vẫn giữ tên file cũ (không có tên khách/ngày) do đổi tên retroactive không khả thi — chỉ ảnh tải lên từ v1.6.3 trở đi mới có tên dạng `tab_tenkhach_ngay_thang_id.jpg`.* |
| **v1.6.3** | 2026-07 | (1) **Đổi cách đặt tên file ảnh** trên Supabase Storage: từ dạng khó đọc `mua_1735500000123.jpg` sang dạng dễ nhận diện `mua_ten_khach_hang_ngay_thang_id.jpg` (VD: `mua_chi_2_30_7_...jpg`) — dễ biết ngay ảnh của giao dịch nào kể cả khi xem trực tiếp trong Supabase Dashboard, không chỉ trong app. Sửa giao dịch mà đổi tên khách/ngày sẽ tự đổi tên file ảnh theo và dọn file ảnh tên cũ, không để lại file mồ côi. (2) Màn **"🖼️ Xem ảnh đã tải lên"** thêm **thanh hiển thị dung lượng đã dùng/còn lại** trong gói Storage miễn phí 1GB (thanh xám = tổng dung lượng, phần xanh = đã dùng), nhãn từng ảnh trong danh sách cũng đổi theo tên mới (hiện tên khách + ngày thay vì chỉ "Thu Mua"/"Bán Ra" chung chung) |
| **v1.6.2** | 2026-07 | Màn "📁 Thư mục lưu trữ Supabase" thêm mục **"🖼️ Xem ảnh đã tải lên"** — liệt kê dạng lưới toàn bộ ảnh đính kèm giao dịch đang có trên Supabase Storage (kèm dung lượng từng ảnh), chạm ảnh để xem phóng to, nút 🗑️ Xoá cho từng ảnh. Xoá 1 ảnh ở đây sẽ xoá cả trên Supabase **lẫn gỡ khỏi đúng giao dịch tương ứng trên máy** (không chỉ xoá file mồ côi, tránh giao dịch còn trỏ tới ảnh đã mất) — cũng yêu cầu nhập mật khẩu giao dịch để xác nhận như khi xoá dữ liệu khác |
| **v1.6.1** | 2026-07 | Sửa lỗi **thông báo (toast) tràn màn hình** khi nội dung dài (VD: thông báo lỗi tải ảnh lên kèm chi tiết lỗi) — trước đây CSS ép chữ nằm 1 dòng duy nhất (`nowrap`) nên thông báo dài bị tràn ra ngoài 2 bên màn hình, đọc không được; nay chữ tự xuống dòng, giới hạn chiều rộng tối đa 88% màn hình. Đồng thời sửa lỗi thông báo bị **cắt ngang/nhấp nháy** khi có nhiều thông báo bắn liên tiếp trong thời gian ngắn (VD: "Đã lưu" → "Đang tải ảnh lên" → báo lỗi) do timer ẩn thông báo cũ không được huỷ đúng cách |
| **v1.6.0** | 2026-07 | **📷 Tính năng mới: Ảnh đính kèm giao dịch (Thu Mua/Bán Ra)** — chụp ảnh trực tiếp hoặc chọn ảnh có sẵn trong máy khi lưu (hoặc sửa) giao dịch, ảnh tự nén nhỏ (tối đa cạnh 1024px, JPEG 72%) trước khi tải lên **Supabase Storage** (KHÔNG dùng chung bảng dữ liệu `mucaosu_data`, tránh làm nặng phần đồng bộ chính). Xem lại ảnh bằng cách chạm icon 📷 trong bảng giao dịch → phóng to toàn màn hình. **Tự động xoá ảnh sau N ngày** để giải phóng dung lượng — số ngày N cài đặt tại ⚙️ Cài đặt → "📷 Ảnh đính kèm giao dịch", **chốt cố định ngay tại thời điểm tải ảnh lên** theo đúng N đang cài lúc đó: sau này đổi N sang giá trị khác **chỉ áp dụng cho ảnh tải lên sau khi đổi**, hoàn toàn không ảnh hưởng tới hạn tự xoá của ảnh đã có từ trước. Xoá ảnh cũng dọn theo khi xoá hẳn giao dịch hoặc bấm ✕ bỏ ảnh lúc đang sửa. ⚠️ *Cần Tài tự tạo 1 bucket Storage tên `giao-dich-anh` trên Supabase Dashboard (xem hướng dẫn bên dưới) trước khi dùng được tính năng này — xem thêm mục "📷 Thiết lập Supabase Storage cho ảnh đính kèm" phía dưới trong file này.* |
| **v1.5.9** | 2026-07 | Màn "📁 Thư mục lưu trữ Supabase": (1) **Ẩn hoàn toàn** các mục dữ liệu hệ thống (Hạn dùng license, Mật khẩu quản trị, Mật khẩu giao dịch, Mã khách hàng) khỏi danh sách hiển thị/xoá — không còn hiện ra để tránh xoá nhầm làm sai lệch license/mật khẩu/mã khách đang dùng, chỉ còn hiện đúng các mục dữ liệu thông thường (giao dịch, sổ gửi, ứng tiền, chấm công...) kèm dòng ghi chú "(đã ẩn N mục hệ thống)". (2) **Thay hộp thoại xác nhận xoá** (trước đây là popup xác nhận thường của trình duyệt) **bằng popup yêu cầu nhập mật khẩu giao dịch** — chỉ xoá được khi nhập đúng mật khẩu giao dịch (hoặc mật khẩu quản trị), tránh xoá nhầm dữ liệu do vô tình chạm phải nút Xoá |
| **v1.5.8** | 2026-07 | (1) **Thêm "📁 Mở thư mục lưu trữ Supabase"** trong màn cài đặt đồng bộ (chạm vào thanh trạng thái kết nối mạng) — liệt kê từng mục dữ liệu đang lưu trên Supabase kèm dung lượng (KB/MB), nút "🗑️ Xoá" cho từng mục để giải phóng bớt dung lượng khi cần (đầy gói miễn phí...). Khi xoá 1 mục: xoá đồng thời cả trên Supabase LẪN trên máy này (localStorage) trong cùng thao tác, tránh vòng lặp "xoá trên Supabase xong lại bị máy tự đồng bộ đẩy dữ liệu cũ lên lại y như trước" — có cảnh báo rõ ràng hơn với các mục dữ liệu hệ thống (license, mật khẩu, mã khách hàng). (2) Màn **"📇 Danh Sách và Mã Khách Hàng"** (đổi tên từ "📇 Mã khách hàng"): đưa khối "🔒 Chốt danh sách Thu Mua" xuống **cuối cùng**, sau danh sách khách hàng (trước đây nằm ngay đầu, phía trên cả ô tìm kiếm). (3) **Âm báo lưu thành công** nay phát đầy đủ ở tất cả các chỗ lưu còn thiếu trong app: lưu thông tin liên hệ khách hàng (SĐT/CCCD/Địa chỉ), lưu sửa khách hàng, thêm khách hàng mới, lưu thông tin xuất bảng kê, và mỗi lần bấm **"⚡ Tính toán & Phân bổ"** (tab Tính Toán) — cùng dùng chung 1 âm thanh và cùng bật/tắt theo đúng 1 cài đặt "🔔 Âm báo lưu thành công" như các chỗ lưu giao dịch khác |
| **v1.5.7** | 2026-07 | (1) **Modal "Xuất Bảng kê thu mua"**: tách tick "Điền ngày xuất, điền ngày ký" thành **2 hàng, 2 tick riêng biệt** — "Điền tháng, năm xuất vào bản kê" và "Điền ngày, tháng, năm ký vào bản kê" — bật/tắt độc lập với nhau thay vì gộp chung 1 tick như trước. (2) **Khắc phục lỗi không tự đồng bộ dữ liệu mới khi mở lại app đã đăng nhập Supabase sẵn**: trước đây việc TỰ ĐỘNG kéo (pull) + trộn dữ liệu mới từ đám mây chỉ chạy khi bấm nút "Đăng nhập" thủ công (`restoreOrPushOnLogin`) — nếu phiên đăng nhập Supabase đã có sẵn từ trước (mở app bằng icon trên điện thoại, không cần đăng nhập lại), app chỉ ĐẨY (push) dữ liệu máy lên nếu có pending, KHÔNG BAO GIỜ tự kéo dữ liệu mới tạo ở thiết bị/phiên khác về — khiến giao dịch tạo ở nơi khác "biến mất" cho tới khi đăng xuất/đăng nhập lại thủ công. Nay mỗi lần mở app (đã đăng nhập Supabase sẵn), sau 3 giây sẽ tự kiểm tra + trộn ngầm dữ liệu mới nhất từ đám mây; chỉ khi THỰC SỰ có dữ liệu mới mới báo "🔄 Đã tự động cập nhật dữ liệu mới…" và tải lại trang, nếu không có gì mới thì chỉ lặng lẽ đẩy phần máy đang có lên (nếu cần), không làm phiền hay tải lại trang vô ích |
| **v1.5.6** | 2026-07 | Màn xem trước Bảng kê thu mua: thêm nút **"📤 Chia sẻ (Zalo, Messenger...)"** dưới hàng In/Tải PDF — dùng Web Share API để mở đúng bảng "Chia sẻ" gốc của điện thoại kèm file PDF vừa dựng, Zalo (và các app khác đã cài như Messenger, Gmail...) sẽ tự xuất hiện trong danh sách để gửi ngay, không cần tải PDF về máy rồi tự đính kèm thủ công. Nếu thiết bị/trình duyệt cũ chưa hỗ trợ chia sẻ kèm file, app báo rõ để dùng "Tải PDF" như trước |
| **v1.5.5** | 2026-07 | Tab **Thu Mua**: khung cuốn chiếu thông tin khách hàng (hiện dưới ô nhập tên khi gõ/chọn đúng 1 khách đã có SĐT/CCCD/Địa chỉ) nay hiện dòng tiêu đề **"Thông tin chi tiết của khách hàng"** làm mục đầu tiên, sau đó mới lần lượt cuốn qua từng thông tin thật (SĐT → CCCD → Địa chỉ) như trước — riêng tab Bán Ra giữ nguyên không đổi |
| **v1.5.4** | 2026-07 | (1) **Tab Tính Toán nay tuân thủ đúng cài đặt "Làm tròn số tiền"**: trước đây khi bấm "⚡ Tính toán & Phân bổ", Thành tiền tính cho từng người bỏ sót bước làm tròn nghìn dù cài đặt đang bật, gây lệch nghìn lẻ so với các tab Mua/Bán/Sổ Gửi/Ứng tiền — nay áp dụng đúng cùng 1 quy tắc làm tròn ở mọi nơi. (2) **Nhắc bổ sung thông tin khách hàng mới ngay sau khi lưu giao dịch**: khi lưu 1 giao dịch (Mua/Bán/Sổ Gửi/Ứng tiền) với tên khách hàng tự gõ tay mà TRƯỚC ĐÓ chưa có trong danh sách khách hàng, sau khi lưu thành công app hiện popup hỏi "Cập nhật thông tin?" — nhấn Huỷ để bỏ qua, nhấn Cập nhật để mở popup điền Số điện thoại/CCCD/Địa chỉ cho đúng khách vừa giao dịch (tên đã điền sẵn), dùng ngay cho lần xuất Bảng kê sau này mà không cần vào lại màn 👥 tìm thủ công |
| **v1.5.3** | 2026-07 | **Khắc phục triệt để lỗi sai số liệu khi xuất "Bảng kê thu mua và thanh toán"**: trước đây bảng kê GỘP nhiều giao dịch của cùng 1 khách hàng trong ngày thành 1 dòng rồi tính lại Số độ trung bình (theo trọng số kg) và suy ngược Đơn giá từ tổng thành tiền — cách làm này làm số liệu trên bảng kê lệch so với "Chi tiết thu mua" thật. Nay bảng kê liệt kê ĐÚNG NGUYÊN từng giao dịch/dòng đã tính toán (có bao nhiêu giao dịch thì kê bấy nhiêu dòng, không cộng dồn), lấy thẳng Kg/Độ/Đơn giá/Thành tiền từ dữ liệu gốc — cột Kg dùng đúng công thức `kgThuc()` (đã trừ bì/khấu hao) y hệt "Chi tiết thu mua" đang hiển thị, cột Đơn giá ở nguồn Tính Toán lấy thẳng đơn giá dùng để phân bổ (không suy ngược) — đảm bảo khớp 100% với bản chi tiết tương ứng theo đúng nguồn đã chọn khi xuất (Thu Mua thật hoặc Tính Toán). Đồng thời sửa khung xem trước bảng kê: trước chỉ kéo được ngang trái/phải, nay **kéo tự do cả 4 chiều** (lên/xuống/trái/phải) bằng 1 ngón tay hoặc chuột, cùng lúc vẫn chụm 2 ngón phóng to/thu nhỏ bình thường |
| **v1.5.2** | 2026-07 | (1) Màn "👥 Danh sách khách hàng" nay hiển thị luôn Số điện thoại/Số CCCD/Địa chỉ ngay dưới Mã khách hàng nếu khách đã có các thông tin này. (2) "➕ Thêm khách hàng mới" đổi thành **popup riêng** thay vì form nhỏ nhúng trong danh sách — gồm Tên khách hàng (bắt buộc, có dấu *), Số điện thoại/Số CCCD/Địa chỉ (đều ghi rõ "có thể bỏ trống" ngay trong placeholder mờ), và 2 tick 🌾 Khách bán / 🚛 Khách mua. (3) Ô nhập tên khách hàng ở tab Thu Mua và Bán Ra: khi gõ/chọn đúng 1 khách đã có Số điện thoại/CCCD/Địa chỉ, phía dưới tên tự hiện icon 🏠 kèm **cuốn chiếu lần lượt từng thông tin** (SĐT → CCCD → Địa chỉ), mỗi thông tin đứng yên 3 giây rồi tự cuộn từ dưới lên đổi sang thông tin kế tiếp |
| **v1.5.1** | 2026-07 | Ảnh xem trước Bảng kê nay **luôn tự động xoay ngang 90°** để hiển thị đầy màn hình, dễ nhìn hơn hẳn (trước đó ở v1.5.0 ảnh hiển thị đúng chiều nhưng nhỏ, để trống nhiều khoảng trắng phía dưới). Lần này xoay đúng 1 tấm ẢNH THẬT (không phải xoay khối HTML sống như các bản trước hay lỗi) nên luôn vừa khít khung, không tràn không thiếu. Vẫn chụm 2 ngón tay phóng to thêm được nếu cần xem chi tiết hơn |
| **v1.5.0** | 2026-07 | **Nâng cấp lớn, sửa triệt để lỗi in ra sai khổ giấy đã xác nhận qua thực tế (test 2 loại máy in đều ra sai)**: nguyên nhân gốc là hộp thoại in hệ thống Android bỏ qua hoàn toàn CSS `@page{size:A4 landscape}`, tự in ra khổ dọc bất kể preview có đúng ngang hay không. Nay bỏ hẳn cách in dựa vào CSS/`window.print()` — nút "🖨️ In" và "⬇️ Tải PDF" đều dùng chung **đúng 1 ảnh chụp thật** (html2canvas) của bảng kê để dựng file PDF khổ A4 ngang thật sự (đọc kích thước trang trực tiếp từ file PDF, không qua CSS nữa nên không thể bị máy in bỏ qua); "In" giờ mở file PDF vừa dựng ra để xem/in/chia sẻ thay vì gọi hộp thoại in hệ thống trực tiếp. Nhờ dùng chung đúng 1 ảnh, **màn xem trước giờ khớp tuyệt đối với bản in/PDF thật** — không còn khác nhau như trước. Màn xem trước cũng đơn giản hoá hẳn: ảnh tự vừa khít bề ngang màn hình tự nhiên (không cần xoay/tính toán co giãn thủ công phức tạp như các bản trước, tránh dứt điểm lỗi tràn màn hình/dạt nút), vẫn chụm 2 ngón tay phóng to được. **Bỏ hẳn nút "📷 Chụp ảnh"** theo yêu cầu (không còn cần thiết vì đã có ảnh xem trước sẵn, có thể chụp màn hình thường nếu cần) |
| **v1.4.9** | 2026-07 | Màn xem trước Bảng kê: **chỉ riêng vùng nội dung (giữa tiêu đề và thanh nút) được xoay ngang 90°** để hiển thị đúng khổ A4 ngang đầy màn hình dễ xem — thanh nút "📷 Chụp ảnh / 🖨️ In / ⬇️ PDF" phía dưới **KHÔNG bị xoay theo nữa**, luôn đứng yên đúng vị trí cố định dưới cùng màn hình dọc như trước giờ vẫn quen dùng. Vẫn giữ chụm 2 ngón tay để phóng to/thu nhỏ thêm nếu cần |
| **v1.4.8** | 2026-07 | **Sửa lỗi màn xem trước Bảng kê bị tràn màn hình và 3 nút Chụp ảnh/In/Tải PDF bị dạt sang lề trái** (do cách xoay 90° trước đây ở v1.4.7 khiến cả thanh nút cũng bị xoay theo, không đúng ý muốn). Bỏ hẳn cách xoay cứng, thay bằng: nội dung bảng kê **tự động co giãn (scale) vừa khít bề ngang màn hình** ngay khi mở xem trước, xem được trọn vẹn mà không cần xoay ngang điện thoại; có thể **chụm 2 ngón tay để phóng to/thu nhỏ** xem chi tiết hơn nếu cần. Thanh nút Chụp ảnh / In / Tải PDF nay **luôn cố định đúng vị trí dưới cùng màn hình dọc** như bình thường, không bị xoay theo nội dung nữa. File PDF/trang in vẫn giữ đúng khổ A4 ngang như trước (không đổi) |
| **v1.4.7** | 2026-07 | **Nâng cấp lớn tính năng Bảng kê thu mua**: (1) **Sửa lỗi tính sai tiền nghiêm trọng** — công thức suy ngược Đơn giá hiển thị trước đây chia nhầm thêm 100 lần (do hiểu sai công thức tính tiền thật của app là kg×độ×giá/100, trong khi thực tế app tính kg×giá×độ KHÔNG chia 100), khiến đơn giá hiển thị cao gấp ~100 lần thực tế — nay đã tính đúng theo chính xác công thức `tinhTien()` thật của app. (2) **Canh giữa toàn bộ dữ liệu trong bảng** (tên, địa chỉ, kg, độ, đơn giá, thành tiền...) thay vì trái/phải như trước. (3) **Sửa lỗi làm tròn số kg sai lệch dữ liệu** — cột Số lượng (kg) trước đây bị làm tròn thành số nguyên (VD 49.3kg → 49kg), nay hiển thị đúng số thập phân chính xác như trong Chi tiết thu mua. (4) Popup "🖨️ Xuất bảng kê": bỏ khoảng "Từ ngày - Đến ngày", thay bằng chọn **đúng 1 ngày** qua lịch mini có đánh dấu chấm các ngày đã có dữ liệu (đổi được nguồn Thu Mua/Tính Toán sẽ đổi luôn ngày được đánh dấu tương ứng); thêm **"Chọn loại mủ"** (Tất cả/Mủ nước/Mủ chén/Mủ dây/Mủ đông) để lọc đúng loại cần xuất; thêm tick **"Điền ngày xuất, điền ngày ký"** — bỏ tick thì cả dòng Tháng/Năm đầu trang lẫn dòng ngày ký cuối trang đều để trống dạng chấm chấm cho viết tay, tick vào thì điền đúng ngày thật. (5) Bản xem trước bảng kê nay **tự động xoay ngang 90°** trên điện thoại cầm dọc (đúng theo khổ giấy A4 ngang thật của bảng kê) để xem rõ hơn, không cần xoay ngang máy. (6) Thêm nút **"📷 Chụp ảnh"** thứ 3 bên cạnh In/Tải PDF — hiện bảng kê toàn màn hình không viền, không nút, không tiêu đề để chụp ảnh sạch; chạm màn hình hiện nút ✕ để thoát, tự ẩn nút sau 5 giây nếu không chạm để không che ảnh chụp. (7) Sửa để nút "⬇️ Tải PDF" luôn chụp đúng từ bản KHÔNG bị xoay (khắc phục việc bản xem trước giờ tự xoay ngang có thể khiến PDF xuất ra bị lệch hướng), luôn ra đúng file PDF khổ A4 ngang |
| **v1.4.6** | 2026-07 | (1) Xác nhận/giữ chắc **định dạng trang mặc định A4 ngang** cho bảng kê ở cả 2 đường xuất: CSS in (`@page{size:A4 landscape}`) và file PDF tải về (`jsPDF orientation:'landscape'`) — riêng nút "⬇️ Tải PDF" luôn ra đúng A4 ngang 100% vì xuất trực tiếp không qua hộp thoại in hệ thống, không phụ thuộc máy in/trình duyệt có tôn trọng đúng CSS hay không (nút "🖨️ In" vẫn qua hộp thoại hệ thống nên có thể bị đổi hướng nếu máy/trình duyệt tự ý ghi đè). (2) Dòng "Tháng .. Năm .. (từ ngày .. đến ngày ..)" trên bảng kê nay **tự bỏ phần "từ ngày - đến ngày"** khi xuất đúng 1 ngày (từ ngày = đến ngày), chỉ còn hiện gọn "Ngày dd/mm/yyyy" |
| **v1.4.5** | 2026-07 | **Sửa lỗi "Máy in này hiện không khả dụng"** khi xuất bảng kê (do hộp thoại in hệ thống tự chọn nhầm máy in vật lý không có sẵn thay vì "Lưu dưới dạng PDF"). Nay bấm "🖨️ Xuất bảng kê" sẽ mở màn **xem trước** đúng nội dung bảng kê ngay trong app (cuộn xem được), kèm 2 lựa chọn: "🖨️ In" (vẫn mở hộp thoại in hệ thống như trước, có thể chọn máy in thật hoặc Lưu PDF) và **"⬇️ Tải PDF"** (mới — xuất thẳng file .pdf tải về máy, KHÔNG cần chọn hay phụ thuộc bất kỳ máy in nào, chữ tiếng Việt hiển thị đúng, tự chia nhiều trang nếu bảng kê dài) |
| **v1.4.4** | 2026-07 | (1) Khung Bảng kê thu mua ở tab Tính Toán đổi sang đúng **tông màu tím** — màu chủ đạo của tab này (giống nút "⚡ Tính toán & Phân bổ", "📜 Lịch sử tính toán"), thay vì màu xanh mặc định; tab Thu Mua giữ nguyên tông xanh lá chủ đạo. (2) Khối "🔍 Kiểm tra đạt yêu cầu" ở tab Tính Toán nay **tự ẩn hoàn toàn** khi mọi chỉ số đặt mục tiêu (kg/độ/tiền) đều đã đạt ≥99% — tiết kiệm không gian hiển thị cho phần khác; chỉ hiện ra khi có ít nhất 1 chỉ số CHƯA đạt yêu cầu |
| **v1.4.3** | 2026-07 | (1) Mục "📄 Bảng kê thu mua & thanh toán" ở tab Thu Mua và tab Tính Toán nay được **bọc trong khung bo viền có thanh tiêu đề màu riêng** (giống phong cách khối màu của app "Xem Sổ Khách Hàng"), thay vì chỉ là 1 đường kẻ phân cách như trước. (2) Xác nhận: tính năng tự động lưu lịch sử tính toán mỗi lần bấm "⚡ Tính toán & Phân bổ" — thay thế đúng kết quả cùng ngày/cùng loại mủ, giữ riêng kết quả nếu khác loại mủ trong cùng ngày — đã có sẵn từ trước (hàm `luuSoSachTuDong()`), không cần chỉnh sửa gì thêm |
| **v1.4.2** | 2026-07 | **Chuyển mục "📄 Bảng kê thu mua & thanh toán" ra khỏi ⚙️ Cài đặt**, đặt trực tiếp ở cuối tab **Thu Mua** (ngay dưới nút "🔍 Xem lại giao dịch cũ") và đầy đủ như vậy ở tab **Tính Toán** (ngay trên "📜 Lịch sử tính toán đã lưu", trước đây chỉ có 1 nút, nay có đủ cả "🏢 Thông tin xuất" + "🖨️ Xuất bảng kê" giống hệt tab Thu Mua). Bấm xuất từ tab Thu Mua thì **luôn tự động chọn nguồn "Dữ liệu Thu Mua"**, bấm từ tab Tính Toán thì mặc định chọn nguồn "Dữ liệu Tính Toán" (vẫn đổi được trong lúc xuất). "Thông tin xuất bảng kê" (Tên doanh nghiệp, MST...) vẫn dùng chung đúng 1 dữ liệu (`bang_ke_info`, đã tự đồng bộ đám mây từ trước) — sửa ở tab nào cũng áp dụng luôn cho tab kia, chỉ là đặt nút bấm ở 2 nơi cho tiện, không phải 2 bộ dữ liệu riêng |
| **v1.4.1** | 2026-07 | Xuất bảng kê: (1) Mặc định khoảng ngày đổi thành **từ hôm nay đến hôm nay** (trước đây mặc định từ đầu tháng), vẫn chỉnh sửa được thoải mái. (2) Thêm **tuỳ chọn nguồn dữ liệu**: "📥 Dữ liệu Thu Mua" (giao dịch thật, mặc định) hoặc "⚡ Dữ liệu Tính Toán" (lấy từ lịch sử đã lưu ở tab Tính Toán trong khoảng ngày chọn). (3) Thêm nút **"🖨️ Xuất bảng kê thu mua"** ngay trong tab Tính Toán, nằm phía trên "📜 Lịch sử tính toán đã lưu" để truy cập nhanh — dùng chung đúng 1 tính năng với mục trong ⚙️ Cài đặt, chỉ khác là mặc định chọn sẵn nguồn "Dữ liệu Tính Toán" khi mở từ đây (vẫn đổi được sang Thu Mua nếu cần) |
| **v1.4.0** | 2026-07 | **Thêm thông tin liên hệ khách hàng + xuất "Bảng kê thu mua và thanh toán mủ cao su" đúng mẫu quy định** (theo file mẫu người dùng cung cấp): (1) Màn "➕ Thêm khách hàng mới" và "✏️ Sửa khách hàng" nay có thêm 3 trường tuỳ chọn: Số điện thoại, Số CCCD/CMND, Địa chỉ — lưu riêng theo từng khách hàng (khoá `kh_info`, có đồng bộ đám mây), không bắt buộc điền. (2) ⚙️ Cài đặt có thêm mục "📄 Bảng kê thu mua & thanh toán" gồm: "🏢 Thông tin xuất" (điền 1 lần: Tên doanh nghiệp, Mã số thuế, Địa chỉ, Địa điểm thu mua, Địa chỉ nơi tổ chức thu mua, Người phụ trách thu mua, Đơn giá mủ nước/mủ tạp — lưu khoá `bang_ke_info`) và "🖨️ Xuất bảng kê" (chọn khoảng ngày + số bảng kê, mặc định từ đầu tháng đến hôm nay). Bảng kê gộp đúng theo từng khách hàng có giao dịch Thu Mua trong khoảng ngày đã chọn (mủ nước tính Số độ trung bình theo trọng số kg riêng với mủ tạp/chén/dây/đông tính theo kg), đầy đủ Địa chỉ/CCCD/SĐT nếu đã điền, thành tiền luôn khớp đúng dữ liệu giao dịch thật đã lưu. Xuất ra dưới dạng bản in trên toàn màn hình (dùng tính năng In/Lưu PDF có sẵn của trình duyệt) để giữ nguyên đúng bố cục mẫu gốc, chữ tiếng Việt không bị lỗi phông |
| **v1.3.1** | 2026-07 | Thêm tuỳ chọn **"🔆 Màn hình luôn bật"** trong ⚙️ Cài đặt (dùng Wake Lock API của trình duyệt) — **mặc định BẬT**, chỉ tắt khi người dùng tự tay tắt trong Cài đặt. Tự xin cấp lại ngay khi quay về app (mở lại tab/mở khoá màn hình) nếu tuỳ chọn vẫn đang bật, vì trình duyệt tự nhả quyền này mỗi khi ẩn tab. Đây là tuỳ chọn riêng của từng thiết bị, không đồng bộ qua lại giữa các máy |
| **v1.3.0** | 2026-07 | Icon logo hình tròn ở góc trên bên trái (header) nay tự xoay liên tục kiểu **đồng xu đang quay** (xoay quanh trục dọc bằng CSS 3D `rotateY`, tự thu hẹp bề ngang giữa chừng vòng quay giống hệt đồng xu thật, không phải chỉ xoay phẳng 2D) |
| **v1.2.9** | 2026-07 | **Sửa lỗi khách hàng đã xoá lại xuất hiện trở lại sau khi tải lại trang.** Nguyên nhân thật sự: khách hàng còn giao dịch Mua/Bán thật thì mỗi lần tải trang, hệ thống tự tính lại danh sách theo đúng giao dịch thật (đúng thiết kế, để không "mồ côi" dữ liệu) — nhưng trước đây bước này không biết khách vừa bị xoá thủ công nên tự thêm lại vào danh sách quản lý. Nay có thêm danh sách "đã xoá thủ công" (đồng bộ lên đám mây), hễ đã xoá thì không tự hiện lại nữa dù vẫn còn giao dịch thật (lịch sử giao dịch vẫn được giữ nguyên, chỉ ẩn khỏi màn quản lý 👥) — trừ khi có hoạt động MỚI thật sự với đúng tên đó (thêm lại thủ công hoặc phát sinh giao dịch mới) thì tự động hiện lại. Đồng thời **sửa lỗi xoá Sổ Gửi trước đây quên đồng bộ lên đám mây hoàn toàn**. Mọi hành động xoá (giao dịch Mua/Bán, Sổ Gửi, Ứng tiền, Chấm công, Khách hàng...) nay đều đồng bộ NGAY lên đám mây kèm thông báo rõ cho người dùng biết |
| **v1.2.8** | 2026-07 | **Sửa lỗi hộp thoại xác nhận (VD "Xoá khách hàng?") bị hiện SAU/DƯỚI các modal khác đang mở** (như modal "Danh sách khách hàng" 👥 và modal "Sửa khách hàng" ✏️), khiến không nhìn thấy để bấm — phải đóng hết các modal phía trên mới thấy. Nguyên nhân: modal xác nhận dùng chung (`modal-cfm`) chưa được gán `z-index` riêng nên mặc định thấp hơn các modal như Sửa khách hàng (đã có z-index cao hơn để hiện trên Danh sách khách hàng). Nay đã gán z-index cao hơn mọi modal thông thường trong app để luôn hiện đúng phía trên cùng, kể cả khi được mở lồng bên trong modal khác |
| **v1.2.7** | 2026-07 | Nâng cấp hộp thoại xử lý xung đột Mã khách hàng khi đăng nhập (thêm ở v1.2.6): thay vì chỉ có 2 lựa chọn cố định, nay có **menu chọn 3 chiến lược riêng cho từng khách hàng bị trùng** — "🔀 Trộn dữ liệu trên máy và đám mây làm một" (mặc định, giữ lại toàn bộ dữ liệu cả 2 bên), "☁️ Đồng bộ dữ liệu trên đám mây, xoá dữ liệu trên máy" (chỉ giữ đúng phần dữ liệu của riêng khách này đã có trên đám mây, bỏ phần mới thêm trên máy chưa từng đồng bộ), "📱 Đồng bộ dữ liệu trên máy lên đám mây, xoá dữ liệu đã có trên đám mây" (ngược lại, giữ đúng dữ liệu trên máy, bỏ phần trên đám mây). Có thêm **tuỳ chọn "Áp dụng cho tất cả các khách hàng khác"** (mặc định để trống, chỉ áp dụng cho khách hiện tại) — tick vào thì chiến lược vừa chọn được áp dụng luôn cho mọi khách hàng còn lại đang bị trùng, không cần xử lý từng người một |
| **v1.2.6** | 2026-07 | **Đổi toàn bộ cơ chế đồng bộ khi đăng nhập từ "ghi đè" sang "TRỘN dữ liệu 2 chiều"**: trước đây, đăng nhập trên máy đang có dữ liệu sẽ đẩy lên đè hết dữ liệu đám mây, còn máy trống thì tải về đè hết dữ liệu máy — cả 2 kiểu đều có thể làm mất dữ liệu ở 1 trong 2 bên. Nay mỗi lần đăng nhập, app tự kiểm tra dữ liệu đã có trên đám mây rồi **hợp (không xoá) với dữ liệu đang có trên máy**: các giao dịch Mua/Bán/Sổ Gửi/Ứng tiền/Chấm công được hợp theo từng bản ghi (không trùng, không thiếu), các dữ liệu dạng danh sách (vai trò thủ công, mốc chốt, lịch sử Tính Toán) được bổ sung thêm khoá mới từ máy vào khoá đã có trên đám mây (không đụng khoá cũ). Riêng **Mã khách hàng**: nếu phát hiện cùng 1 tên khách hàng có mã khác nhau giữa máy và đám mây (VD: tạo khách mới lúc chưa đăng nhập, trùng tên với khách đã có mã cấp trên đám mây từ trước) — app sẽ **hỏi rõ người dùng** có muốn thay mã trên máy bằng mã đã có trên đám mây hay không, chứ không tự ý chọn, tránh làm sai lệch mã đã phát cho khách hàng thật |
| **v1.2.5** | 2026-07 | (1) Đổi nhãn hiển thị mã dùng để **gia hạn/kích hoạt phần mềm** từ "Mã khách hàng" thành **"Mã thiết bị"** ở modal 🔑 Gia hạn, mục Hướng dẫn sử dụng và dòng thông tin giấy phép trong ⚙️ Cài đặt — để không còn trùng tên gọi với "Mã khách hàng" (mã 5 chữ số của khách hàng dùng để tra cứu sổ sách trong app "Xem Sổ Khách Hàng"), tránh nhầm lẫn giữa 2 khái niệm mã hoàn toàn khác nhau. (2) **Sửa lỗi nghiêm trọng gây mất/đổi Mã khách hàng**: hàm chuyển đổi mã một lần (mã chữ+số cũ → mã thuần số mới) trước đây, nếu thiếu cờ đánh dấu "đã chuyển đổi" trên máy (VD vừa khôi phục dữ liệu từ Supabase trên trình duyệt/thiết bị mới, hoặc sau khi xoá lịch sử duyệt web), sẽ SINH MÃ MỚI NGẪU NHIÊN ĐÈ LÊN toàn bộ Mã khách hàng đang dùng dù mã đó đã đúng chuẩn số — khiến khách hàng không tra cứu được sổ sách bằng mã cũ đã được đưa. Nay chỉ sinh mã mới khi thực sự phát hiện còn mã kiểu cũ. (3) **Mở rộng cơ chế đồng bộ Supabase để quét TOÀN BỘ khoá dữ liệu nghiệp vụ đang có trong máy** (không chỉ giới hạn ở danh sách `SB_DATA_KEYS` khai báo sẵn — nay tự động gộp thêm mọi khoá localStorage khác của app, trừ số ít khoá chỉ-riêng-máy-này như token Google Drive/mã thiết bị), tránh sót dữ liệu của các tính năng mới thêm sau mà lỡ quên khai báo. (4) Đồng bộ Google Drive: thêm sheet "Sao_Luu_Day_Du" dump toàn bộ dữ liệu nghiệp vụ dạng khoá-giá trị JSON thô làm lưới an toàn bổ sung, phòng trường hợp 1 loại dữ liệu chưa có sheet định dạng riêng |
| **v1.2.4** | 2026-07 | Thêm icon `purpose: "maskable"` (512×512) vào `manifest.json` — chuẩn bị cho việc đóng gói app thành file APK Android qua TWA (Trusted Web Activity), giúp icon hiển thị đúng chuẩn "adaptive icon" của Android thay vì bị cắt/méo. Không ảnh hưởng gì đến bản PWA đang dùng trên trình duyệt |
| **v1.2.3** | 2026-07 | Đổi tên **"Mã máy"** thành **"Mã khách hàng"** ở toàn bộ nơi hiển thị liên quan đến gia hạn thời gian sử dụng (modal 🔑 Gia hạn, dòng thông tin giấy phép trong ⚙️ Cài đặt, mục Hướng dẫn sử dụng) cho chuyên nghiệp hơn — không đổi tên biến/khoá lưu trữ nội bộ (device_code, getDeviceCode) để tránh ảnh hưởng dữ liệu cũ |
| **v1.2.2** | 2026-07 | (1) Modal "📋 Xem chi tiết thu mua trong ngày": bỏ hẳn tổng số kg ở card "Tổng cộng tất cả loại" (và cả khi lọc theo 1 loại riêng) — vì số tiền thu mua trong ngày thường gồm nhiều loại mủ khác nhau nên cộng chung kg không có ý nghĩa, chỉ giữ số lượt giao dịch + tổng tiền. (2) Thêm mục "ℹ️ Giới thiệu phần mềm" trong ⚙️ Cài đặt, nằm ngay dưới "📖 Hướng dẫn sử dụng" — giới thiệu chi tiết mục đích, các chức năng chính, app đồng hành, cơ chế lưu trữ/đồng bộ, bảo mật và thông tin liên hệ hỗ trợ |
| **v1.2.1** | 2026-07 | (1) Tab Thu Mua: đọc to số tiền **ngay khi bấm nút "✅ Lưu giao dịch"** phía dưới phần nhập liệu, không còn phải chờ bấm "Lưu" trong popup xác nhận mới đọc. (2) Thêm **âm báo "ting"** khi lưu dữ liệu thành công, áp dụng ở tất cả các nơi có nút Lưu trong app: Lưu giao dịch & Lưu sửa giao dịch (tab Mua/Bán), Lưu sổ gửi, Lưu ứng tiền, Sửa ghi chép sổ gửi, Tạo chấm công mới, Lưu ghi chép chấm công. Thêm mục "🔔 Âm báo lưu thành công" trong ⚙️ Cài đặt để bật/tắt (mặc định **BẬT** khi cài app mới hoặc sau khi xoá lịch sử duyệt web). Âm báo dùng Web Audio API dựng sẵn (2 nốt "ting"), không cần file âm thanh ngoài nên hoạt động cả khi offline |
| **v1.2.0** | 2026-07 | Tab Thu Mua: sau khi bấm "Lưu giao dịch" thành công, tự động đọc to số tiền bằng giọng nói (không cần bấm nút loa 🔊 nữa). Thêm mục "🔊 Tự động đọc số tiền" trong ⚙️ Cài đặt để bật/tắt riêng tính năng này. Số tiền được đọc là số đã được làm tròn theo cài đặt "Làm tròn số tiền" tại thời điểm lưu. Đổi mặc định của cả 2 cài đặt "Làm tròn số tiền" và "Tự động đọc số tiền" thành **luôn BẬT sẵn** khi cài đặt app mới hoặc sau khi xoá lịch sử duyệt web (trước đây "Làm tròn số tiền" mặc định tắt) |
| **v1.1.9** | 2026-07 | Bỏ nút "🔄 Đổi khách hàng khác" ở đầu kết quả popup "Xem lại giao dịch cũ" (tab Thu Mua) cho thoáng, đỡ trống trải — muốn tra cứu khách khác thì đóng popup rồi mở lại như bình thường |
| **v1.1.8** | 2026-07 | Tinh chỉnh popup "🔍 Xem lại giao dịch cũ" (tab Thu Mua) theo phản hồi thực tế: (1) Bỏ dòng "Tổng ngày: X kg" ở mỗi khối ngày — vì 1 ngày có thể gồm nhiều loại mủ khác nhau (nước/chén/dây/đông) nên cộng chung kg không có ý nghĩa, chỉ giữ lại tổng tiền của ngày đó. (2) Sau khi chọn tên từ danh sách gợi ý xổ xuống hoặc gõ đầy đủ tên rồi nhấn "Tra cứu" (tìm thấy dữ liệu), tự động ẩn khung nhập tên + nút Tra cứu, chỉ hiện phần kết quả (từ tên khách hàng trở xuống) chiếm trọn màn hình cho dễ xem; có thêm nút "🔄 Đổi khách hàng khác" ở đầu kết quả để quay lại ô nhập tìm người khác mà không cần đóng/mở lại popup |
| **v1.1.7** | 2026-07 | Thêm nút "🔍 Xem lại giao dịch cũ" ở tab Thu Mua, nằm ngay dưới khối "📊 Thống kê trong ngày". Mở ra popup có ô nhập tên khách hàng (gõ ký tự đầu là xổ ra danh sách gợi ý tên đúng theo khách Thu Mua, chọn nhanh không cần gõ hết) + nút "🔍 Tra cứu" — hiện toàn bộ giao dịch Thu Mua **mọi thời điểm** (không chỉ trong ngày) của khách đó, tách rõ theo từng ngày riêng biệt (mỗi ngày 1 khối có ngày/thứ + số lượt + bảng chi tiết Loại/Kg/Độ/Giá/Tiền/trạng thái Gửi sổ + tổng kg-tiền riêng của ngày đó), kèm khối tổng hợp tổng lượt/tổng ngày/tổng kg/tổng tiền ở trên cùng |
| **v1.1.6** | 2026-07 | Bỏ banner "🔄 Đã có bản cập nhật mới — Nhấn để tải lại" — nay phát hiện bản mới là **tự động tải lại ngay**, không cần chạm vào thông báo nữa |
| **v1.1.5** | 2026-07 | Sửa lỗi "Chưa có lịch sử nào được lưu cho ngày/loại mủ này!" khi xoá ghi chép ngay trên chính lịch sử vừa mở lại: nguyên nhân do hàm lưu/xoá/xem chi tiết (`luuSoSachTuDong`, `xoaGhiChepSS`, `xemChiTietSS`) đang lấy nhầm ngày theo tab Thu Mua (`curDate['mua']`) thay vì đúng ngày đang chọn ở ô "Ngày tính toán" — 2 ngày này độc lập với nhau nên khi xem lịch sử của 1 ngày khác ngày hiện tại ở tab Thu Mua là bị lệch, tìm không ra để xoá. Nay cả 3 hàm đều đọc đúng qua hàm dùng chung `getSsDate()` |
| **v1.1.4** | 2026-07 | Sửa ô "Loại mủ" và "Ngày tính toán" (tab Tính Toán) về cùng chiều cao — trước đó ô ngày bị cao hơn do dòng chữ "Nhấn để chọn" tràn xuống dòng riêng trên màn hẹp; đã bỏ dòng chữ đó (không cần thiết, cả khung đã rõ là bấm được) và đặt chiều cao cố định bằng nhau cho cả 2 ô. *(Đã kiểm tra kỹ manifest.json/sw.js cho lỗi "chỉ thêm được phím tắt, không cài được icon app" — code hoàn toàn hợp lệ, không tìm thấy lỗi; nghi vấn cao nhất là do file icon-192.png/icon-512.png trên GitHub bị mất/lỗi, cần anh tự kiểm tra lại trên GitHub Pages)* |
| **v1.1.3** | 2026-07 | (1) Sửa lỗi hiển thị "82,800000000000" và "NaN%" khi mở lại lịch sử tính toán: dòng kiểm tra "Tổng tiền" từng so sánh 2 chuỗi đã định dạng dấu chấm (VD "1.825.129") thay vì số thật, khiến phép chia ra NaN — nay tính đúng bằng số thật, chỉ định dạng lúc hiển thị; ô "Tổng kg cần đạt" khi tải lại lịch sử cũng được làm tròn, không còn hiện sai số thập phân do dấu phẩy động. (2) Bỏ nút "💾 Lưu Sổ Sách" thủ công — nay **mỗi lần nhấn "⚡ Tính toán & Phân bổ" sẽ tự động lưu vào Lịch sử tính toán ngay**, tính lại nhiều lần trong cùng ngày + loại mủ sẽ tự thay thế lần trước, không trùng lặp. Thay vào đúng vị trí đó là nút mới "🗑️ Xoá ghi chép" — dùng khi mở lại 1 lịch sử cũ và muốn xoá hẳn. (3) Sửa lại đúng nội dung Hướng dẫn sử dụng: kết quả Tính Toán chỉ là số liệu tham khảo, KHÔNG ghi đè vào giao dịch Thu Mua thật (bản hướng dẫn trước ghi nhầm) |
| **v1.1.2** | 2026-07 | Thêm "📜 Lịch sử tính toán đã lưu" ở cuối tab Tính Toán: mỗi lần nhấn "💾 Lưu Sổ Sách" nay hiện thêm 1 mục vào danh sách lịch sử (dữ liệu này thực ra đã được lưu sẵn ở khoá `ss_data` từ trước, chỉ là chưa có nơi xem lại) — nhấn nút mở ra danh sách các lần đã lưu (ngày + loại mủ + tổng kg/tiền), chạm vào 1 dòng sẽ tự đổ toàn bộ dữ liệu (mục tiêu đã nhập + bảng kết quả) trở lại lên trên để xem lại. Đã thêm `ss_data` vào danh sách đồng bộ Supabase (trước đây chỉ lưu máy + xuất riêng qua Google Sheets, chưa đồng bộ JSON chung) |
| **v1.1.1** | 2026-07 | 🔒 Sửa lỗi có thể mất hạn dùng sau khi xoá lịch sử duyệt web rồi đăng nhập lại Supabase: nguyên nhân do `getLicenseExpiry()` tự khởi tạo ngay giá trị "hết hạn hôm nay" cục bộ khi chưa có dữ liệu, và nếu app kịp tự đồng bộ lên Supabase trước khi cơ chế xác minh vân tay thiết bị (`verifyLicenseAntiCheat`) kịp khôi phục đúng hạn dùng, giá trị sai sẽ ghi đè mất hạn dùng thật đang lưu trên tài khoản. Nay gọi xác minh vân tay thiết bị ngay khi phát hiện phiên đăng nhập (cả lúc mở app lẫn lúc đăng nhập thủ công) và trì hoãn 1.2–3 giây trước khi cho phép tự đồng bộ/khôi phục, đảm bảo hạn dùng đúng được khôi phục trước |
| **v1.1.0** | 2026-07 | 🔄 Đổi hệ đánh số phiên bản: từ "1.<chữ><số>" (VD 1.b5) sang "1.X.Y" — Y chạy 0→9 rồi tăng X, Y về 0 lại (VD: 1.1.0 → 1.1.1 → ... → 1.1.9 → 1.2.0). Mốc khởi điểm hệ mới là 1.1.0, không liên quan tới nội dung thay đổi tính năng |
| **v1.b5** | 2026-07 | Cập nhật toàn diện màn "📖 Hướng dẫn sử dụng": thêm mục ⏱️ Chấm Công (trước đây chưa có), viết lại đúng chức năng thật của tab 🧮 Tính Toán (mục tiêu cần đạt + phân bổ lại + Lưu Sổ Sách, trước đây ghi nhầm là "tính nhanh không lưu"), thêm mục mới 👥 Quản lý khách hàng & Mã khách hàng (mã số, vai trò tự động, thêm/sửa/xoá, chốt theo khách hàng), cập nhật mục Đồng bộ theo đúng cơ chế mới (tự đồng bộ ngầm 15s + 3 trạng thái thanh kết nối), sửa mẹo sai (trước ghi nhầm "nhấn logo 🌿 để mở hướng dẫn" — thực tế phải vào Cài đặt) |
| **v1.b4** | 2026-07 | Bỏ badge trạng thái dư thừa "Đã kết nối" ở thanh đồng bộ trên cùng (bị trùng lặp thông tin, dễ nhìn nhầm). Gộp toàn bộ thông tin vào đúng 1 dòng chữ chi tiết, giống app "Xem Sổ Khách Hàng": icon ⚡ Supabase / ☁️ Google Drive + "✅ Đã kết nối - cập nhật lúc: HH:MM:SS ngày dd/mm" khi đồng bộ thành công, "⚠️ Mất kết nối" khi mất mạng/chưa đăng nhập (kèm số mục chưa sao lưu nếu có), "❌ Lỗi đồng bộ" khi đồng bộ thất bại |
| **v1.b3** | 2026-07 | (1) **Đồng bộ ngầm định kỳ mỗi 15 giây**: quét toàn bộ dữ liệu có thể đồng bộ (kể cả những chỗ trước đây lỡ quên gọi đồng bộ, ví dụ thêm/sửa/xoá khách hàng) và tự đồng bộ ngầm nếu 15 giây không có thao tác nào (chạm/gõ) — hoàn toàn im lặng, không chớp màn hình, không toast, lặp lại liên tục nếu vẫn tiếp tục im lặng; vẫn đồng bộ ngay lập tức mỗi khi lưu giao dịch thành công như trước. Đây là lưới an toàn giúp không còn bị thiếu dữ liệu (như thiếu tên khách hàng) khi xoá lịch sử duyệt web rồi đăng nhập lại. (2) Thêm nhãn "Mã khách hàng" phía trên mỗi mã ở màn 👥, tránh nhầm không biết dãy số đó là gì. (3) Đổi cách tạo Mã khách hàng sang **thuần số 5 chữ số** (trước đây có cả chữ lẫn số, VD 7FDNP) — thay thế TOÀN BỘ mã cũ bằng mã số mới ngay khi mở app, đồng bộ đồng thời với app "Xem Sổ Khách Hàng" (cũng đã cập nhật placeholder + bàn phím số) và tự đẩy mã mới lên Supabase ngay. (4) Bỏ nút "📇 Mã khách hàng" trong ⚙️ Cài đặt vì đã có đầy đủ (kèm thêm/sửa/xoá) ở icon 👥 |
| **v1.b2** | 2026-07 | Thiết kế lại triệt để cách xác định "🌾 Khách bán"/"🚛 Khách mua": bỏ hẳn cách "dọn dữ liệu 1 lần" của bản v1.b1 (vẫn có thể bị lệch nếu có giao dịch test/thay đổi sau mốc dọn). Nay có hàm `rebuildKHRoleLists()` tính lại HOÀN TOÀN kh3_mua/kh3_ban trực tiếp từ giao dịch thật (gd3_mua/gd3_ban) mỗi khi cần hiển thị danh sách khách hàng hoặc gợi ý tên — đúng tuyệt đối theo giao dịch tại mọi thời điểm, tự sửa vĩnh viễn không cần "dọn" nữa: khách chỉ có giao dịch ở tab Thu Mua → chỉ "Khách bán"; chỉ có giao dịch ở tab Bán Ra → chỉ "Khách mua"; có giao dịch cả 2 tab mới lên cả 2 dấu. Khách thêm/gán vai trò thủ công (chưa có giao dịch) lưu riêng ở khoá `kh_role_manual`, không còn ghi trực tiếp đè lên kh3_mua/kh3_ban nên không thể gây lẫn nữa. Màn sửa khách hàng (✏️) cũng khoá sẵn ô vai trò nào đã có giao dịch thật tương ứng (không tắt được, chỉ có thể tick thêm vai trò còn lại) |
| **v1.b1** | 2026-07 | (1) Sửa lỗi bản v1.a9 chưa dọn được dữ liệu cũ: hàm dọn dữ liệu (xây lại đúng vai trò Khách bán/Khách mua) trước đó vô tình bị đặt bên trong `switchTab()`, chỉ chạy khi mở tab Tính Toán — nay chuyển ra chạy ngay khi mở app, không phụ thuộc tab nào. (2) Bỏ cơ chế tự động điền lại tên người mua gần nhất theo từng loại mủ ở tab Bán Ra — cơ chế này khiến ô "Nhập tên..." tưởng như không tự xoá được (vừa lưu xong, chọn lại loại mủ là bị điền lại tên cũ); nay ô tên luôn trống sau khi lưu giao dịch, đúng như ô tên ở tab Thu Mua |
| **v1.a9** | 2026-07 | (1) Sửa lỗi mọi khách hàng đều bị gắn cả 2 nhãn "🌾 Khách bán" và "🚛 Khách mua" cùng lúc: nguyên nhân do khi lưu tên (dù ở tab Mua, Bán, Sổ Gửi hay Ứng tiền) đều tự động ghi vào CẢ HAI danh sách nội bộ `kh3_mua`/`kh3_ban`; nay chỉ ghi đúng vào danh sách của tab đang lưu, đồng thời gợi ý tên (autocomplete) ở tab Mua/Bán cũng chỉ lấy đúng danh sách của tab đó — không còn lẫn tên qua lại. Có chạy dọn dữ liệu cũ 1 lần khi mở app: xây lại đúng vai trò dựa trên giao dịch Mua/Bán thực tế đã có. (2) Gộp 2 màn quản lý khách hàng làm 1: icon 👥 nay mở thẳng màn "📇 Mã khách hàng" (trước đây mở màn "Danh sách khách hàng" riêng, tách biệt và thiếu đồng bộ với màn Mã khách hàng ở Cài đặt). Màn này nay có thêm khối "➕ Thêm khách hàng mới" (nhập tên + chọn vai trò Khách bán/Khách mua) và nút ✏️ cạnh nút 📋 Sao chép ở mỗi dòng để mở popup sửa tên/vai trò, lưu hoặc xoá khách hàng ngay tại đây |
| **v1.a8** | 2026-07 | (1) Khoá đúng cách 2 khu vực **Chấm Công** và **Tính Toán** khi hết hạn/chưa kích hoạt — trước đây chỉ chặn được thao tác lưu ở tầng code (guardLicense) nhưng giao diện vẫn không mờ/khoá trực quan như Mua/Bán/Sổ Gửi; phát hiện thêm nút "Lưu Sổ Sách" bị mất id `tt-luu-btn` từ trước nên khoá chưa từng có tác dụng — đã khôi phục và bổ sung banner khoá cho cả 2 khu vực. (2) Sửa công thức **Thực lãnh** trong Chấm Công: trước đây tính bằng cả tháng lương trừ nghỉ/ứng cộng thưởng (sai với kỳ đang mở, chưa hết tháng); nay tính đúng theo **số ngày đã làm thực tế × tiền công 1 ngày**, trừ ứng cộng thưởng — phản ánh đúng số tiền thực tế tính đến hiện tại. (3) Màn "📇 Mã khách hàng" nay phân biệt rõ 3 vai trò bằng nhãn màu riêng: 👷 Công nhân (vàng), 🌾 Khách bán (xanh dương — người bán mủ cho vựa), 🚛 Khách mua (xanh lá — đầu mối mua lại mủ từ vựa) |
| **v1.a7** | 2026-07 | Sửa lỗi app đứng yên ở bản cũ dù đã tải file mới lên GitHub: thêm banner "🔄 Đã có bản cập nhật mới" tự hiện khi phát hiện bản mới (app tự kiểm tra mỗi khi mở lại), bấm vào để tải lại đúng bản mới nhất — không cần gỡ cài đặt hay xoá cache thủ công nữa |
| **v1.a6** | 2026-07 | (1) "Chốt danh sách Thu Mua" đổi sang chọn **ngày chốt tuỳ ý** qua lịch mini có đánh dấu ngày có giao dịch (● đỏ), mặc định là hôm nay, có thể đổi sang ngày khác/tháng khác. (2) Công nhân trong Chấm Công nay cũng được cấp **Mã KH riêng** (đồng bộ sang app "Sổ Khách Hàng" để tự tra cứu kỳ chấm công của mình). (3) Màn "👥 Danh sách khách hàng" (CRUD) nay hiển thị luôn Mã KH kèm nút 📋 sao chép nhanh, không cần mở riêng màn "📇 Mã khách hàng" nữa |
| **v1.a5** | 2026-07 | Đồng bộ thêm mật khẩu quản lý giao dịch lên Supabase (khoá `thang_pw`) để app "Xem Sổ Khách Hàng" có thể xác thực trước khi cho khách hàng mở menu Cài đặt của họ (tránh bấm nhầm thoát/đổi mã) |
| **v1.a4** | 2026-07 | Sửa lỗi bấm "Chốt danh sách Thu Mua" không có phản hồi: modal xác nhận mật khẩu bị modal Mã khách hàng đè lên (cùng z-index, modal Mã khách hàng nằm sau trong DOM nên vẽ đè lên trên) — nay modal xác nhận mật khẩu luôn ở lớp trên cùng. Đồng thời đổi cơ chế "Chốt danh sách Thu Mua" từ chốt chung 1 mốc duy nhất sang **chốt riêng theo từng khách hàng**: bấm nút sẽ mở popup có menu xổ xuống chọn tên khách hàng, hiện trạng thái đã/chưa chốt của người đó, rồi mới chốt/huỷ chốt (`mua_chot_moc` đổi cấu trúc từ 1 số thành object `{ "Tên KH": mốc }`) |
| **v1.a3** | 2026-07 | (1) Sửa lỗi mã khách hàng bị nhân đôi khi xoá rồi thêm lại khách hàng trùng tên khác hoa/thường (VD "anh 2" và "Anh 2"): mọi so khớp tên khách hàng (thêm mới, sửa tên, tra mã) nay không phân biệt hoa/thường và khoảng trắng thừa; màn "📇 Mã khách hàng" chỉ hiện khách đang tồn tại, không còn hiện mã mồ côi của khách đã xoá; đổi tên khách hàng nay di chuyển đúng mã cũ sang tên mới thay vì sinh mã mới. (2) Thêm cơ chế "🔒 Chốt danh sách Thu Mua" trong màn Mã khách hàng: khi chốt (yêu cầu mật khẩu quản lý giao dịch), app "Xem Sổ Khách Hàng" sẽ chỉ hiện các giao dịch Thu Mua phát sinh SAU thời điểm chốt — dữ liệu cũ vẫn được giữ nguyên đầy đủ trong app này, chỉ ẩn khỏi app khách hàng. Có thể "🔓 Huỷ chốt" bất cứ lúc nào. (3) Thay toàn bộ logo/icon ứng dụng và ảnh khởi động (splash) bằng bộ nhận diện thương hiệu mới |
| **v1.a2** | 2026-07 | Thêm `admin_pw` (mã quản trị) vào danh sách đồng bộ Supabase, kèm giá trị mặc định đúng khi chưa từng đổi mật khẩu — phục vụ tính năng "chế độ quản trị" sắp có ở app "Xem Sổ Khách Hàng" (nhập mã quản trị vào ô Mã khách hàng sẽ được xem toàn bộ khách hàng thay vì chỉ 1 người) |
| **v1.a1** | 2026-07 | Đổi cơ chế đánh số phiên bản: chuyển từ v11.14 sang hệ mới `1.<chữ><số>` (xem quy tắc phía trên) do số phiên bản cũ đã quá cao. Không có thay đổi tính năng nào trong bản này |
| **v11.14** | 2026-07 | Thêm hệ thống "Mã khách hàng" cố định (5 ký tự, không đụng cấu trúc kh3_all cũ nên không ảnh hưởng autocomplete hiện có) — mỗi khách hàng tự động có 1 mã khi tên được lưu. Thêm màn "📇 Mã khách hàng" trong Cài đặt để tra cứu/sao chép mã, chuẩn bị cho app đồng hành "Xem Sổ Khách Hàng" (máy khách) sắp triển khai — khách hàng nhập đúng mã này để xem sổ Thu Mua/Gửi/Ứng của riêng mình. Đồng bộ mã KH lên cả Supabase (`kh_ids`) và Google Drive (sheet `Ma_KH`) |
| **v11.13** | 2026-07 | Thêm 3 tuỳ chọn lọc mới trong menu "Xem dữ liệu Supabase": "📒 Theo Gửi sổ (chọn khách hàng)", "💵 Theo Ứng tiền (chọn khách hàng)", "⏱️ Theo Chấm công (chọn công nhân)" — mỗi tuỳ chọn chỉ hiện đúng 1 loại dữ liệu và có ô chọn tên riêng để xem chính xác người/công cần xem. Tên công nhân (Chấm công) dùng ô chọn hoàn toàn tách biệt với tên khách hàng (Gửi sổ/Ứng tiền/Mua/Bán), không gộp chung danh sách nữa như v11.12 |
| **v11.12** | 2026-07 | Xác nhận dữ liệu Chấm Công đã tự động đồng bộ Supabase (khoá `cc_data`) từ trước — mọi thao tác tạo/ghi chép/chốt/xoá đều gọi autoSync() ngay. Bổ sung phần "⏱️ Chấm Công" vào màn "Xem dữ liệu Supabase": thẻ tổng quan từng công nhân (ngày làm/nghỉ, ứng, thưởng, thực lãnh, trạng thái mở/chốt) và bảng ghi chép chi tiết (ngày, công nhân, loại, số tiền, lý do) có thể lọc theo ngày/tháng/tên công nhân giống các mục khác; đưa tên công nhân vào chung dropdown lọc theo khách hàng; thêm mục Chấm Công vào file PDF xuất từ Supabase |
| **v11.11** | 2026-07 | Xác nhận lỗi "Chốt công/Xoá chấm công" ở v11.10 là do trình duyệt/PWA chưa cập nhật cache, không phải lỗi code (đã hoạt động đúng sau khi xoá lịch sử/cache trình duyệt). Thêm lưới an toàn try/catch quanh 2 nút này để nếu có lỗi bất ngờ trong tương lai sẽ luôn hiện thông báo rõ ràng thay vì im lặng |
| **v11.10** | 2026-07 | Sửa lỗi nghiêm trọng: nút "Chốt công" và "Xoá chấm công" không có phản ứng khi bấm xác nhận — nguyên nhân do cơ chế nút back Android chỉ theo dõi 1 modal, khi popup xác nhận (mở chồng lên modal chi tiết chấm công) đóng lại thì lại đóng nhầm luôn modal chi tiết bên dưới, khiến người dùng tưởng không có gì xảy ra. Đã viết lại toàn bộ cơ chế thành ngăn xếp modal (hỗ trợ nhiều modal chồng nhau đúng cách). Làm lại màn tổng quan Chấm Công: tách riêng từng chỉ số (ngày làm, ngày nghỉ, tiền ứng, tiền thưởng, trừ nghỉ, thực lãnh) với màu rõ ràng (ứng = đỏ, thưởng = xanh), thêm nhãn trạng thái "Đang mở"/"Đã chốt". Thêm kiểm tra giấy phép (guardLicense) và thông báo lỗi rõ ràng thay vì im lặng cho toàn bộ thao tác Chấm Công |
| **v11.9** | 2026-07 | Nâng cấp lớn màn chi tiết Chấm Công: lịch hiển thị 5 trạng thái mỗi ngày (✔ có làm mặc định, ✗ nghỉ, 💵 ứng tiền, 💵 thưởng tiền, ❗ khác); nhấn vào ngày đã đánh dấu sẽ đổ dữ liệu xuống khung nhập liệu bên dưới để sửa (chọn ngày/loại ghi chép/số tiền nếu là ứng-thưởng/lý do), lưu có popup xác nhận. Thêm phần "Chi tiết đầy đủ" (số ngày làm/nghỉ, tiền trừ nghỉ, tiền ứng, tiền thưởng, thực lãnh) và nút "✅ Chốt công" (có popup xác nhận) để khoá ghi chép tháng đó; muốn tính tháng mới cho cùng 1 người thì tạo chấm công mới với tên đó — hệ thống tự thêm hậu tố "(Tháng X/Y)" và chặn tạo trùng nếu kỳ cũ chưa chốt |
| **v11.8** | 2026-07 | Thêm sub-tab "⏱️ Chấm Công" trong tab Sổ Gửi (giữ nguyên sub-tab "📒 Sổ Gửi & Ứng Tiền" như cũ). Nút "➕ Tạo chấm công mới" mở popup nhập: Ngày (mặc định hôm nay, chọn qua lịch tùy chỉnh có đánh dấu), Tên công nhân, Tiền công tháng — tự tính Tiền công/ngày = Tiền công tháng ÷ 30. Mỗi công nhân có màn chi tiết với lịch để đánh dấu ngày nghỉ, tự động trừ tiền các ngày nghỉ và tính ra Thực lãnh. Dữ liệu Chấm Công đồng bộ cả Supabase (`cc_data`) và Google Drive (sheet `Cham_Cong`) |
| **v11.7** | 2026-07 | Sửa lỗi các nút trong menu ⚙️ Cài đặt (Gia hạn, Đổi mật khẩu, Quên mật khẩu, Hướng dẫn sử dụng) bấm không có phản ứng — nguyên nhân do cơ chế nút back Android đóng nhầm modal vừa mở khi chuyển tiếp từ modal Cài đặt; đã thêm hàm chuyển đổi modal an toàn (swapM) để khắc phục |
| **v11.6** | 2026-07 | Màn xem dữ liệu Supabase: khi lọc theo ngày/tháng/khách hàng, thêm hàng "Tổng cộng" in đậm ở cuối mỗi bảng (Mua/Bán/Sổ gửi/Ứng tiền); khi chọn "Xem tất cả", chèn hàng in đậm ngăn cách giữa các ngày khác nhau trong bảng để dễ phân biệt |
| **v11.5** | 2026-07 | Viết lại cơ chế nút quay lại điện thoại: đóng popup đang mở ở lần bấm đầu, thoát app tự nhiên ở lần bấm tiếp theo (bỏ popup xác nhận thoát vì trình duyệt không cho phép JS tự đóng app đáng tin cậy); bỏ tuỳ chọn bật/tắt tự đồng bộ trong Cài đặt — luôn tự động đồng bộ ngay khi có giao dịch/thay đổi cài đặt nếu đã đăng nhập Supabase/Drive; đổi màu số trừ bì trong bảng danh sách sang đỏ nhạt dễ đọc hơn; rút gọn tên loại mủ (Chén/Dây/Đông) trong cột Độ/Loại của bảng Chi tiết thu mua trong ngày |
| **v11.4** | 2026-07 | Sửa lỗi nút "Thoát ứng dụng" không phản hồi (đơn giản hoá lại cơ chế thoát, bỏ window.close() gây gián đoạn); làm nét icon màn hình khởi động — giảm padding, bỏ chế độ "maskable" khiến hệ điều hành tự thu nhỏ thêm gây mờ |
| **v11.3** | 2026-07 | Thay toàn bộ icon app (logo header, apple-touch-icon, icon-192, icon-512) bằng logo mới, đã xử lý xoá nền và làm sạch cho hiển thị đẹp trên mọi kích thước; thêm popup xác nhận khi bấm nút quay lại của điện thoại để tránh thoát app nhầm |
| **v11.2** | 2026-07 | Lần đầu cài đặt/mở app hiện popup "Kích hoạt phần mềm" (thay vì báo hết hạn) yêu cầu nhập mã, khoá phần nhập liệu cho đến khi kích hoạt; banner khoá hiện "Nhấn vào đây để kích hoạt phần mềm" (màu xanh) khi chưa từng kích hoạt, đổi thành "Phần mềm đã hết hạn — Nhấn để gia hạn" (màu đỏ) khi đã từng dùng rồi hết hạn; popup thông báo hết hạn có thêm nút "Gia hạn ngay"; sửa lỗi form không tự mở khoá sau khi gia hạn/kích hoạt thành công; logo chính giảm về đúng chiều cao 2 hàng chữ, không vượt quá như trước |
| **v11.1** | 2026-07 | Logo chính phóng to bao phủ cả 2 hàng chữ (tiêu đề + email/phiên bản/hạn dùng); icon Cài đặt/Khách hàng thu nhỏ, căn theo hàng tiêu đề, không lấn xuống hàng dưới; Hướng dẫn sử dụng bỏ hết nội dung liên quan "dùng thử", chỉ còn hướng dẫn gia hạn |
| **v11.0** | 2026-07 | Thu nhỏ icon Cài đặt/Khách hàng (bằng độ rộng icon logo), giữ cùng hàng với tiêu đề, giảm khoảng cách các hàng header để dành nhiều không gian hơn cho phần nhập liệu; xoá bỏ hoàn toàn ngày dùng thử miễn phí — cài đặt app xong khoá ngay, bắt buộc gia hạn mới sử dụng được (dữ liệu hạn dùng vẫn đồng bộ lên Supabase như cũ); cập nhật lại hướng dẫn sử dụng cho đúng |
| **v10.9** | 2026-07 | Icon Cài đặt và Khách hàng chuyển lên cùng hàng với tên app, hàng dưới chỉ còn email/phiên bản/hạn dùng cho rộng rãi hơn; thanh trạng thái đồng bộ đổi tên tài khoản (chiếm nhiều diện tích) thành "Đã kết nối mạng - sẵn sàng lưu trữ online" kèm icon phương án đang dùng, tên tài khoản vẫn xem được khi bấm vào; cập nhật đầy đủ Hướng dẫn sử dụng với phần Supabase (đăng nhập, xem dữ liệu, lọc, xuất PDF) và mốc dùng thử mới; gắn hạn dùng thử vào tài khoản Supabase (ngoài vân tay thiết bị) để khôi phục đáng tin cậy hơn khi đăng nhập lại sau khi xoá dữ liệu duyệt web |
| **v10.8** | 2026-07 | Ẩn thông tin mật khẩu khỏi lịch sử phiên bản; đổi tên nhãn "mật khẩu chốt sổ" thành "mật khẩu giao dịch" trong giao diện Hủy chốt sổ cho nhất quán |
| **v10.7** | 2026-07 | Thanh trạng thái Supabase hiện rõ "Đang đồng bộ..." khi đang xử lý và "Lỗi đồng bộ (n)" kèm số bản ghi chưa đồng bộ khi thất bại (tự thử lại ở lần ghi chép/cài đặt tiếp theo); thay đổi cài đặt (làm tròn số, tự đồng bộ) giờ cũng được đồng bộ lên Supabase; màn xem dữ liệu ghi chép và PDF bỏ cột "Loại mủ" — gộp vào cột "Độ" (hiện Chén/Dây/Đông thay số độ, riêng mủ nước vẫn hiện số độ); định dạng ngày thông minh: cùng năm hệ thống hiện dd/mm, khác năm hiện dd/mm/yyyy |
| **v10.6** | 2026-07 | Tái cấu trúc triệt để hàm Gia hạn: đóng popup + hiện thông báo thành công chạy ngay sau khi xác thực (không còn phụ thuộc các bước phụ như đồng bộ server/kiểm tra hạn); mọi thao tác phụ đều bọc an toàn; bọc toàn bộ hàm trong try/catch kèm thông báo lỗi rõ ràng nếu có sự cố, tránh treo im lặng |
| **v10.5** | 2026-07 | Sửa lỗi popup Gia hạn không đóng và không hiện thông báo thành công khi dùng mật khẩu quản trị (dù ngày vẫn được cộng đúng) — đổi thứ tự xử lý để đóng popup/thông báo trước khi chạy kiểm tra phụ; sửa lỗi ô nhập mật khẩu gia hạn nhận sai chế độ (hiện nút "Gia hạn" thay vì "Áp dụng") khi gõ mật khẩu quản trị |
| **v10.4** | 2026-07 | Thêm cơ chế chống gian lận hạn dùng thử (chống xoá dữ liệu duyệt web để reset vô hạn) dựa trên dấu vân tay thiết bị + bảng `device_trials` trên Supabase; xác nhận lại phân quyền mật khẩu: mật khẩu quản trị = cao nhất (gồm gia hạn, chốt sổ, mọi thao tác), mật khẩu giao dịch = chỉ chốt sổ/quản lý giao dịch, không gia hạn được |
| **v10.3** | 2026-07 | Đổi định dạng ngày trong màn xem dữ liệu Supabase và file PDF xuất ra sang dd/mm/yyyy; sửa công thức tính hạn dùng thử — ngày cài đặt không tính vào 3 ngày dùng thử (dùng miễn phí thêm ngày cài đặt, tổng 4 ngày truy cập trước khi khoá) |
| **v10.2** | 2026-07 | Bộ lọc "Theo khách hàng" đổi thành "Theo khách hàng (chưa chốt sổ)" — ẩn các ghi chép Sổ gửi/Ứng tiền đã chốt sổ; thêm tuỳ chọn mới "Toàn bộ dữ liệu của khách hàng" (hiện cả dữ liệu đã chốt sổ); ô "Theo ngày" đổi sang lịch tuỳ chỉnh có chấm đánh dấu ngày có ghi chép giống các tab khác |
| **v10.1** | 2026-07 | Thêm bộ lọc xem dữ liệu Supabase theo Ngày / Tháng / Khách hàng; gộp nút "Xem dữ liệu ghi chép" + "Tải PDF chi tiết" thành 1 mục; xuất PDF đúng theo đúng dữ liệu đang được lọc trên màn hình |
| **v10.0** | 2026-07 | Thêm màn "Xem dữ liệu ghi chép" ngay trong app cho phương án Supabase (xem Mua/Bán/Sổ gửi/Ứng tiền dạng bảng, có tổng tiền từng mục); thêm xuất báo cáo PDF chi tiết tải về điện thoại; thêm nút hiện/ẩn mật khẩu (👁️) khi đăng nhập Supabase |
| **v9.9** | 2026-07 | Thêm phương án đồng bộ **Supabase** (đăng nhập email/mật khẩu, dùng chung nhiều thiết bị) hoạt động song song với Google Drive; thêm modal chọn phương án đồng bộ khi bấm thanh trạng thái; tự động đồng bộ dữ liệu lên Supabase ngay sau mỗi giao dịch/sổ gửi/ứng tiền; giữ nguyên 100% chức năng Google Drive hiện có |
| **v9.8** | — | Phiên bản gốc trước khi bổ sung Supabase (mốc bắt đầu ghi tiếp lịch sử phiên bản từ đây) |
| **v1.3** | 2026-05 | Sửa lỗi đọc số tiền bằng chữ; dropdown chọn loại mủ xổ xuống tại chỗ + auto nhảy ô; thêm icon Sổ Gửi với đầy đủ chi tiết, sửa/xoá; popup xác nhận khi lưu/gửi; cột Chất hiện tên loại mủ cho mủ chén/dây |
| **v1.2** | 2026-05 | Sửa thứ tự ô, công thức tính, lỗi mủ chén, thêm cột Kg |
| **v1.1** | 2026-05 | Sửa công thức, ghi nhớ đơn giá theo loại mủ |
| **v1.0** | 2026-05 | Phiên bản đầu tiên |

## ✨ Tính năng

### Nhập giao dịch
- **Loại mủ** — dropdown xổ tại chỗ, chọn xong tự nhảy sang ô Tên
- **Đơn giá** — ghi nhớ theo từng loại mủ; để trống nếu loại mới
- **Tên người bán/mua** — gợi ý autocomplete
- **Số kg + Độ / Khấu hao** (tùy loại mủ)

### Công thức tính
| Loại | Công thức |
|------|-----------|
| Mủ nước | `kg × giá × độ` |
| Mủ đông | `(kg − khấu hao) × giá` |
| Mủ chén / Dây | `kg × giá` |

### Popup xác nhận
Khi **Lưu giao dịch** hoặc **Ghi sổ gửi** → hiện popup chi tiết: Tên, Loại mủ, Số kg, Đơn giá, Độ/KH, Thành tiền

### Bảng giao dịch
- Cột Chất: mủ nước → Độ, mủ đông → Khấu hao, mủ chén/dây → tên loại
- Nhấp đúp dòng → chỉnh sửa (tô vàng sau khi sửa)
- Nhấp đúp cột Gửi → popup xác nhận → ghi sổ gửi ✅

### Sổ Gửi 📒
- Nhấn icon 📒 trên header → danh sách khách đã gửi
- Xem chi tiết từng khách: ngày, loại, kg, độ, giá, tiền — tổng cộng
- Nhấp đúp ghi chép → chỉnh sửa / xoá

### Khách hàng 👥
- Mua và bán có danh sách riêng
- Thêm / Sửa / Xoá

## 📱 Cài lên điện thoại

### Deploy GitHub Pages (miễn phí)
1. Tạo repo mới trên github.com
2. Upload toàn bộ 6 file
3. **Settings → Pages → Branch: main → Save**
4. Link: `https://username.github.io/ten-repo/`

### Cài như app
| Hệ điều hành | Cách |
|---|---|
| **Android** | Chrome → Menu ⋮ → *Thêm vào màn hình chính* |
| **iPhone** | Safari → Nút chia sẻ ↑ → *Thêm vào màn hình chính* |

## 📁 File cần upload lên GitHub
```
mu-cao-su/
├── index.html      ← Toàn bộ app (v1.3)
├── manifest.json   ← Cấu hình PWA
├── sw.js           ← Service Worker (offline)
├── icon-192.png    ← Icon app
├── icon-512.png    ← Icon splash
└── README.md
```
> 💾 Dữ liệu lưu trong **localStorage** — không cần server, hoạt động offline hoàn toàn.

## 📷 Thiết lập Supabase Storage cho ảnh đính kèm (bắt buộc từ v1.6.0)

Tính năng đính kèm ảnh giao dịch (chụp ảnh/chọn ảnh trong máy) cần 1 **bucket Storage riêng** trên Supabase — bucket này KHÔNG tự tạo được từ trong app (cần quyền quản trị dự án), Tài cần vào **Supabase Dashboard** tạo 1 lần duy nhất theo các bước sau:

**Bước 1 — Tạo bucket:**
1. Vào Supabase Dashboard → dự án đang dùng → **Storage** (thanh bên trái)
2. **New bucket** → đặt tên chính xác: `giao-dich-anh`
3. Bật **Public bucket** (để ảnh hiển thị được trực tiếp qua link, không cần đăng nhập) → **Create bucket**

**Bước 2 — Cho phép tải lên/xoá ảnh (RLS Policy):** vào **SQL Editor** → dán đoạn sau → **Run**:
```sql
create policy "giao-dich-anh insert" on storage.objects for insert to authenticated
with check (bucket_id = 'giao-dich-anh' and (storage.foldername(name))[1] = auth.uid()::text);

create policy "giao-dich-anh update" on storage.objects for update to authenticated
using (bucket_id = 'giao-dich-anh' and (storage.foldername(name))[1] = auth.uid()::text);

create policy "giao-dich-anh delete" on storage.objects for delete to authenticated
using (bucket_id = 'giao-dich-anh' and (storage.foldername(name))[1] = auth.uid()::text);
```
Đoạn này cho phép **đúng tài khoản Supabase đang đăng nhập trong app** (Tài) được tải lên/sửa/xoá ảnh trong đúng thư mục của mình (`{user_id}/...`), không ai khác tải/xoá được. Việc **xem ảnh** thì không cần policy riêng vì bucket đã bật Public ở Bước 1.

Nếu bỏ qua 2 bước trên, khi bấm 📷 Chụp ảnh/🖼️ Chọn ảnh app vẫn lưu giao dịch bình thường, chỉ riêng bước tải ảnh lên sẽ báo lỗi (toast "⚠️ Lỗi tải ảnh lên") — không mất dữ liệu giao dịch.

> ℹ️ Ảnh đính kèm hiện chỉ hiển thị được trong chính app **"Mua Bán Mủ Cao Su"** (chạm icon 📷 trong bảng giao dịch). App đồng hành **"Xem Sổ Khách Hàng"** cần được cập nhật riêng (thêm đoạn hiển thị `<img>` từ field `anh` trong dữ liệu) mới hiện được ảnh cho khách xem — gửi kèm file đó ở lần chỉnh sửa sau nếu muốn bổ sung.

